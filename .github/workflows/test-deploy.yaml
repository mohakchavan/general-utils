# This workflow file is used only for testing of workflows.
#
# DO NOT SUBMIT CHANGES FOR THIS FILE.
name: Test Deploy

on:
  workflow_dispatch:

jobs:

  test-deploy:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout
        uses: actions/checkout@v6
        with:
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            pom.xml
            mvnw
            .mvn

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          java-version: 21
          distribution: "temurin"
          cache: "maven"

      - name: Show Settings
        shell: sh
        run: |
          echo "${{secrets.KEYSTORE_FILE}}" | base64 --decode --ignore-garbage > general-utils-keystore.p12
          ls -als
          ls -als .mvn

      - name: Build
        env:
          ALIAS: ${{secrets.KEYSTORE_ALIAS}}
        shell: sh
        run: |
          echo "$ALIAS"
          echo "::add-mask::Alias"
          chmod +x mvnw
          ./mvnw clean validate dependency:resolve
          

